import {combineReducers} from "redux";

const initialState = {};

const app = (state=initialState, action) => {
    switch(action.type) {

    }
    return state;
}

export default combineReducers({
    app
})